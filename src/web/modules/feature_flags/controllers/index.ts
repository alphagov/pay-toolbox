import * as overviewController from './overview.controller'
import * as walletsController from './frontend/wallets.controller'

export const overview = overviewController
export const frontend = {
  wallets: walletsController
}
